# Public Folder

Place your static files like `logo.png` in this folder.
Vite will automatically copy files from this directory to the root of the build output (`dist/`).

For example, your logo image should be saved at:
`public/logo.png`
